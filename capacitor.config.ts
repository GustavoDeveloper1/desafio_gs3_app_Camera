import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'challenge_galery',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
